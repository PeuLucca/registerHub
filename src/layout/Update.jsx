import React from 'react';
import { SyncOutlined } from '@ant-design/icons';

const UpdateIcon = ({ style, spin }) => {
  return <SyncOutlined style={style} spin={spin} />;
};

export default UpdateIcon;
